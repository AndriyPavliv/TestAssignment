@{
    ViewBag.Title = "_EmployeeSalesList";
}
<div class="container">
    <div class="row-fluid">
        <h2>Employee Sales</h2>
    </div>
    <div class="row-fluid">
        <div class="pull-right">
            @{Html.RenderPartial("EmployeeSales/_EmployeeSalesSearch");}
        </div>        
    </div>
    <div class="row-fluid">
        <!-- ko if: EmployeeSales().length != 0-->
        <div>
            @{ Html.RenderPartial("EmployeeSales/_EmployeeSalesTable"); }
        </div>
        <!-- /ko -->
        <!-- ko ifnot: EmployeeSales().length != 0-->
        <div>
            @{ Html.RenderPartial("_NoResult"); }
        </div>
        <!-- /ko -->       
    </div>
</div>